<div class="container">
  <h1>Contact</h1>

  <!-- -->
  <%#  %>

  <%# This will NOT be displayed in the browser %>
  <%# er + tab %>
  <% puts Time.now %>

  <%# This will be displayed in the browser %>
  <%# pe + tab %>

  <form action="/contact" method="get">
    <input type="text" name="member" placeholder="Who are you looking for?" value='<%= params[:member] %>'>
    <input type="submit">
  </form>

  <ol>
    <%#= members.each { |member| "<li>#{member}</li" } %>
    <% @members.each do |member| %>
      <li><%= member.capitalize %></li>
    <% end %>
  </ol>

</div>
